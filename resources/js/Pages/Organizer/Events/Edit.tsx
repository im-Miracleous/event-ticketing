/**
 * Organizer's Edit Event page — renders the same form as Admin/Events/Edit
 * but omits the "Assign Organizer" dropdown since the organizer is implicit.
 */
import EditEvent from '@/Pages/Admin/Events/Edit';
export default EditEvent;
