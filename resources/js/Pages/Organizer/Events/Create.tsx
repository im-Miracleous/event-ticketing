/**
 * Organizer's Create Event page — renders the same form as Admin/Events/Create
 * but omits the "Assign Organizer" dropdown since the organizer is implicit.
 */
import CreateEvent from '@/Pages/Admin/Events/Create';
export default CreateEvent;
